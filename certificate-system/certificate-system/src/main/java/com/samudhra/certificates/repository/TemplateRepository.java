package com.samudhra.certificates.repository; import com.samudhra.certificates.entity.Template; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.JpaRepository; public interface TemplateRepository extends JpaRepository<Template,String>{ Page<Template> findByOrganizationId(String organizationId, Pageable page); }
